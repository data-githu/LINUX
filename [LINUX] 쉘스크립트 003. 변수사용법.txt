쉘스크립트 003. 변수사용법

1. 모든 변수는 문자열(string) 로 취급됩니다.
2. 변수 이름은 대소문자를 구분합니다.
3. 변수에 값을 대입할 때는 '=' 좌우에 공백이 없어야합니다.
4. 변수에 들어간 문자를 출력하려면 변수앞에 $ 를 붙이고 echo 명령어로 출력하면 됩니다.

예 : 
$myvar="Hi~~"
$echo $myvar